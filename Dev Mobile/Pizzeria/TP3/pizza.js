// Tristan Annaval

class Pizza {
    id = 0;
    code = '';
    libelle = '';
    prix = '';

    constructor(id, code, libelle, prix) {
        this.id = id;
        this.code = code;
        this.libelle = libelle;
        this.prix = prix;
    }
}

module.exports.Pizza = Pizza;

// Tristan Annaval